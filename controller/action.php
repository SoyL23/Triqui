<?php
if (isset($_POST['Idtable'])) {
    $Idtable = $_POST['Idtable'];

    // Construir el contenido que deseas imprimir en la celda.
    $nuevoContenido = "";

    // Devolver el nuevo contenido como respuesta, que será manejado por JavaScript.
    echo $nuevoContenido;
}
?>
